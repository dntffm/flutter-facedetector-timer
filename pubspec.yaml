name: tflite_1
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  tflite_flutter: ^0.9.1
  tflite_flutter_helper: ^0.2.1
  camera: ^0.9.8+1
  image: ^3.0.1
  google_ml_kit: ^0.9.0
  fluttertoast: ^8.2.6
  flutter_local_notifications: ^17.2.2
  flutter_background: ^1.2.0
  intl: ^0.18.0


  cupertino_icons: ^1.0.6
  image_picker: ^1.1.2
  google_mlkit_face_detection: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/model_blink.tflite
    - assets/model_eyegaze.tflite
    - assets/tes.jpeg
